<Window x:Class="ArakWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ArakWPF"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        Loaded="Window_Loaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="4*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <StackPanel Orientation="Vertical">
                <Label Content="Termék"
                       HorizontalAlignment="Center"
                       FontSize="24"
                       x:Name="termekNeve"/>
                <ProgressBar x:Name="pbAllapot"
                             Height="20"
                             Width="150"
                             Minimum="-400"
                             Maximum="+400"
                             />
            </StackPanel>
            <Button x:Name="btnTorles"
                    Content="Törlés"
                    Height="30"
                    Margin="10"
                    Click="btnTorles_Click"/>
            <Button x:Name="btnMentes"
                    Content="Mentés"
                    Height="30"
                    Margin="10" Click="btnMentes_Click"/>
        </StackPanel>

        <DataGrid Grid.Row="1"
                  x:Name="dtgAdatok" ColumnWidth="*" SelectionChanged="dtgAdatok_SelectionChanged"/>

    </Grid>
</Window>

